pipeline
{
    agent any
    stages
    {
        stage('countDownload')
        {
            steps
            {
                git "https://github.com/ramkumarvaddi/jenkinsfile.git"
            }
        }
stage('countbuild')
        {
            steps
            {
                sh 'mvn package'
            }
        }
        
    }
}
